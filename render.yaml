services:
  - type: web
    name: alpharadar-service
    env: node
    plan: free
    buildCommand: npm install && pip install -r requirements.txt
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: 18.16.0
